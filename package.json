{
  "name": "guo-news-radio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "episode": "node server/build-episode.mjs",
    "tts": "node server/build-audio.mjs",
    "start": "node server/server.mjs",
    "build": "rm -rf dist && mkdir -p dist && cp prototype/index.html dist/index.html && cp -r public/* dist/"
  },
  "dependencies": {
    "fish-audio": "^0.1.0"
  }
}

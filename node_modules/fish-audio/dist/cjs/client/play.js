"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.play = play;
const FishAudioError_js_1 = require("../errors/FishAudioError.js");
const utils_js_1 = require("./utils.js");
async function play(audio) {
    if (!(0, utils_js_1.isNode)()) {
        throw new FishAudioError_js_1.FishAudioError({
            message: "The play function is only available in a Node.js environment.",
        });
    }
    const { spawn } = await Promise.resolve().then(() => __importStar(require("node:child_process")));
    const { Readable } = await Promise.resolve().then(() => __importStar(require("node:stream")));
    const commandExists = (await Promise.resolve().then(() => __importStar(require("command-exists")))).default;
    if (!commandExists.sync("ffplay")) {
        throw new FishAudioError_js_1.FishAudioError({
            message: `ffplay from ffmpeg not found, necessary to play audio.
            On mac you can install it with 'brew install ffmpeg'.
            On linux and windows you can install it from https://ffmpeg.org/`,
        });
    }
    const ffplay = spawn("ffplay", ["-autoexit", "-", "-nodisp"], {
        stdio: ["pipe", "ignore", "pipe"],
    });
    Readable.from(audio).pipe(ffplay.stdin);
    const errorChunks = [];
    ffplay.stderr.on("data", (chunk) => {
        errorChunks.push(chunk);
    });
    return new Promise((resolve, reject) => {
        ffplay.on("close", (code) => {
            if (code === 0) {
                resolve();
            }
            else {
                const error = Buffer.concat(errorChunks).toString();
                reject(new FishAudioError_js_1.FishAudioError({
                    message: `ffplay exited with code ${code}. Stderr: ${error}`,
                }));
            }
        });
        ffplay.on("error", (err) => {
            reject(new FishAudioError_js_1.FishAudioError({ message: `Failed to start ffplay: ${err.message}` }));
        });
    });
}
//# sourceMappingURL=play.js.map